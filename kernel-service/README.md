# Kernel Service

Privileged executor implementing IOCTL opcodes per `docs/specs/WindowsAgent + KernelService Interface.json`. Includes system control, update staging/commit, attestation, and tamper checks with schema validation stubs and crypto placeholders for academic use.
