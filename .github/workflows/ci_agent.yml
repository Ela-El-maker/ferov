name: Agent Build
on: [push]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: cmake -S windows-agent -B windows-agent/build
      - name: Build
        run: cmake --build windows-agent/build --config Release
