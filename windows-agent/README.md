# Windows Agent

User-mode Windows agent implementing WSS client, telemetry collection, command handling, IOCTL bridge to KernelService, and policy cache. Matches envelopes defined in `docs/specs/WindowsAgent + FastAPI (WSS control channel).json` and IOCTL schema in `WindowsAgent + KernelService Interface.json`.
